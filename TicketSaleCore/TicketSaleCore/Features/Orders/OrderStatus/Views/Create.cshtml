@model TicketSaleCore.Features.Orders.OrderStatus.ViewModels.OrderStatusEditCreateViewModel
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Create new"];
}

<h2>@Localizer["Create"]</h2>

<form class="ui form"
      asp-action="Create" method="post">
    <div class="field">
        <label asp-for="StatusName"></label>
        <input asp-for="StatusName" type="text" placeholder="@Localizer["name"]" />
    </div>
  
    <div class="ui blue submit button">@Localizer["Create"]</div>
    
</form>
@Html.ModelStateValidMsge(ViewData.ModelState, Localizer)
<div>
    <a asp-action="Index">@Localizer["Back to List"]</a>
</div>

<script type="text/javascript">
    $('.ui.form')
        .form({
            on: 'blur',
            inline: true,
            fields: {
                name: {
                    identifier: 'StatusName',
                    rules: [
                        {
                            type: 'empty',
                            prompt: '@Localizer["Please enter name"]'
                        }
                    ]
                }
            }
        });
</script>

