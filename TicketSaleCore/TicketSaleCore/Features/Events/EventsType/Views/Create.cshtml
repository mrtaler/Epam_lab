@model TicketSaleCore.Features.Events.EventsType.ViewModels.EventsTypeEditCreateViewModel
@using Microsoft.AspNetCore.Mvc.Localization
@using TicketSaleCore.Features.Events.EventsType.ViewModels
@using TicketSaleCore.Features.Orders.OrderStatus.ViewModels
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Create new City"];
}

<h2>@Localizer["Create"]</h2>

<form class="ui form"
      asp-action="Create" method="post">
    <div class="field">
        <label asp-for="NameEventsType"></label>
        <input asp-for="NameEventsType" type="text" name="Name" placeholder="@Localizer["name"]" />
    </div>
  
    <div class="ui blue submit button">@Localizer["Create"]</div>
    
</form>
@Html.ModelStateValidMsge(ViewData.ModelState, Localizer)
<div>
    <a asp-action="Index">@Localizer["Back to List"]</a>
</div>

<script type="text/javascript">
    $('.ui.form')
        .form({
            on: 'blur',
            inline: true,
            fields: {
                name: {
                    identifier: 'Name',
                    rules: [
                        {
                            type: 'empty',
                            prompt: '@Localizer["Please enter name"]'
                        }
                    ]
                }
            }
        });
</script>

